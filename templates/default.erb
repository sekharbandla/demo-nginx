server {
        listen <%= node['demo-nginx']['listen'] %>;
        server_name <%=node['demo-nginx']['nginx-privateip']>;
 location / {
       proxy_pass http://<%= node['demo-nginx']['proxypass'] %>:8080;
        }
}

